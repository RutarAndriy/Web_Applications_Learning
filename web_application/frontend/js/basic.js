// Зберігання даних у localStorage
function save_data (collection) {

}

// Завантаження даних з localStorage
function load_data (collection) {

}

// Створення нового елемента
function create_element() {
   console.log(`Create: ${location.pathname}`);
}

// Пошук існуючого елемента
function find_element (input) {
   console.log(`Find: ${location.pathname}`);
}

// Редагування існуючого елемента
function edit_element (button) {
   console.log(`Edit: ${button}`);
}

// Видалення існуючого елемента
function delete_element (button) {
   console.log(`Delete: ${button}`);
}